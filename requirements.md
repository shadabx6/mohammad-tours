## Packages
framer-motion | Smooth animations for list entry and form interactions
react-hook-form | Form state management
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
- "Clean Minimalist" aesthetic: ample whitespace, sans-serif typography, subtle shadows.
- Schema lacks a timestamp, so messages will be displayed without dates (or minimal visual treatment).
